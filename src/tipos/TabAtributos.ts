export enum ModoOpciones {
  MANUAL = 'manual',
  API = 'api'
}

export enum MetodoHttp {
  GET = 'GET',
  POST = 'POST'
}

export enum ModoCarga {
  REEMPLAZAR = 'reemplazar',
  AGREGAR = 'agregar'
}
